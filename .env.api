# Server-Modus des Frontends (geladen von `npm run dev:api` bzw. `vite --mode api`):
# echter Login + Daten über die REST-API statt Mock im Browser. Kein Geheimnis — nur ein Schalter.
VITE_API_MODE=server
